spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.99.100:3306/testdb  # docker-machine ip default
    username: root
    password: password
    platform: org.hibernate.dialect.MySQL5InnoDBDialect

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: false
    show-sql: true
